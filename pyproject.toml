[project]
name = "Case Cracker"
version = "0.1.0"
description = "Enter data to predict the outcome of a case."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "scikit-learn>=1.7.2",
    "streamlit>=1.52",
]
